services:
  - type: web
    name: my-react-app
    env: static
    buildCommand: npm run build
    startCommand: npm run start
    staticDirs:
      - ./dist
    routes:
      - src: /static/*
        dest: /static/
      - src: /api/*
        dest: /api/
      - src: /*
        dest: /
